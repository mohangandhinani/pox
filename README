*************Commands to execute ************
1)First run the controller using the below command
sudo ./pox.py misc.nat log.level --DUBUG

2) Run this command to start the topology
  sudo python test.py

3)Append ips to block in the blocked.txt file
  allowed ips are - 192.168.1.10,192.168.1.11,192.168.1.12
